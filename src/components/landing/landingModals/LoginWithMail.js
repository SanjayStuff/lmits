import React, { useState } from "react";

const LoginWithMail = () => {
  const [open, setOpen] = useState(false);

  return <h3>Login with Mail, or Phone Number with Password</h3>;
};

export default LoginWithMail;
